语法:
   int ord    ( string $string   )
说明:
返回字符串string第一个字符的ASCII码值。
参数:
  参数              string                  一个字符。                     
返回值:
  返回值     返回整型的 ASCII 码值。   
示例:

<?php
$str = "\n";
if (ord($str) == 10) {
    echo "The first character of \$str is a line feed.\n";
}
?>

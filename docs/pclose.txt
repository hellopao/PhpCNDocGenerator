语法:
   int pclose    ( resource $handle   )
说明:
关闭用popen()打开的指向管道的文件指针。
参数:
  参数              handle                  文件指针必须有效，且必须是成功调用   popen() 所返回的。                     
返回值:
  返回值     返回运行的进程的终止状态。发生错误时会返回 -1。   
示例:

<?php
$handle = popen('/bin/ls', 'r');
pclose($handle);
?>

语法:
   float hypot    ( float $x   , float $y   )
说明:
hypot()函数将会跟据直角三角形的两直解边长度x和y计算其斜边的长度。或者是从标点(x,y)到原点的距离。该函数的算法等同于sqrt(x*x+y*y)。
参数:
  参数              x                  第一条边的长度                        y                  第二条边的长度                     
返回值:
  返回值     计算斜边的长度   
示例:

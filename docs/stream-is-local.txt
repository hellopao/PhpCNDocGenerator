语法:
   bool stream_is_local    ( mixed $stream_or_url   )
说明:
Checksifastream,oraURL,isalocaloneornot.
参数:
  参数              stream_or_url                  The stream resource or URL to check.                     
返回值:
  返回值     成功时返回 TRUE， 或者在失败时返回 FALSE。   
示例:

     Basic usage example.
    
<?php
var_dump(stream_is_local("http://example.com"));
var_dump(stream_is_local("/etc"));
?>
以上例程的输出类似于：
bool(false)
bool(true)

语法:
    bool is_infinite     ( float $val    )
说明:
如果val为无穷大（正的或负的），例如log(0)的结果或者任何超出本平台的浮点数范围的值，则返回TRUE。
参数:
  参数              val                  要检查的值                     
返回值:
  返回值     如果 val     为无穷大返回 TRUE，否则返回 FALSE。   
示例:

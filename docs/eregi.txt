语法:
   int eregi    ( string $pattern   , string $string   [, array &$regs  ] )
说明:
本函数和ereg()完全相同，只除了在匹配字母字符时忽略大小写的区别。
示例:

<?php
$string = 'XYZ';
   if (eregi('z', $string)) {
    echo "'$string' contains a 'z' or 'Z'!";
}
?>

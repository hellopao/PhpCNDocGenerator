语法:
   int mcrypt_enc_get_key_size    ( resource $td   )
说明:
返回打开的模式所能支持的最长密钥，以字节为单位。
参数:
  参数              td                  加密描述符。                     
返回值:
  返回值     返回打开的模式所能支持的最长密钥，以字节为单位。   
示例:

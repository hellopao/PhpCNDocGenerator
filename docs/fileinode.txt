语法:
   int fileinode    ( string $filename   )
说明:
取得文件的inode。
参数:
  参数              filename                  文件的路径。                     
返回值:
  返回值     返回文件的 inode 节点号，  或者在失败时返回 FALSE。   
示例:

<?php
$filename = 'index.php';
if (getmyinode() == fileinode($filename)) {
    echo 'You are checking the current file.';
}
?>

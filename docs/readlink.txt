语法:
   string readlink    ( string $path   )
说明:
readlink()和同名的C函数做同样的事，返回符号连接的内容。
参数:
  参数              path                  链接符号的路径。                     
返回值:
  返回值     返回链接的路径内容，出错则返回 FALSE。   
示例:

<?php
// output e.g. /boot/vmlinux-2.4.20-xfs
echo readlink('/vmlinuz');
?>

语法:
   string ob_get_clean    ( void   )
说明:
得到当前缓冲区的内容并删除当前输出缓冲区。
返回值:
  返回值     返回输出缓冲区的内容，并结束输出缓冲区。如果输出缓冲区不是活跃的，即返回 FALSE 。   
示例:

<?php
ob_start();
echo "Hello World";
$out = ob_get_clean();
$out = strtolower($out);
var_dump($out);
?>
以上例程会输出：
string(11) "hello world"

语法:
   int zip_entry_compressedsize    ( resource $zip_entry   )
说明:
返回指定目录项压缩过后的大小。
参数:
  参数              zip_entry                  由函数zip_read() 返回的目录项。                     
返回值:
  返回值     压缩后的大小。   
示例:
